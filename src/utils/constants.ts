const UI_COMPONENT_ATTRS = Object.freeze({
  BUTTON: 'button',
});

const LETTER_STATUS = Object.freeze({
  NONE: 'none',
  CORRECT: 'correct',
  PRESENT: 'present',
  ABSENT: 'absent',
});

const WORDLE_GAME_MAX_GUESSES = 6;

export { UI_COMPONENT_ATTRS, LETTER_STATUS, WORDLE_GAME_MAX_GUESSES };
